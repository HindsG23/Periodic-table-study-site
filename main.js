function checkAnswer() {
// Set the vaue of the underlined word
partOfSpeech = 'C';

// Get the user's input value
userAnswer = document.getElementById('userAnswer').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer2() {
// Set the vaue of the underlined word
partOfSpeech = 'P';

// Get the user's input value
userAnswer = document.getElementById('userAnswer2').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message2');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message2');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer3() {
// Set the vaue of the underlined word
partOfSpeech = 'Si';

// Get the user's input value
userAnswer = document.getElementById('userAnswer3').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message3');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message3');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer4() {
// Set the vaue of the underlined word
partOfSpeech = 'N';

// Get the user's input value
userAnswer = document.getElementById('userAnswer4').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message4');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message4');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer5() {
// Set the vaue of the underlined word
partOfSpeech = 'Zn';

// Get the user's input value
userAnswer = document.getElementById('userAnswer5').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message5');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message5');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer6() {
// Set the vaue of the underlined word
partOfSpeech = 'Fe';

// Get the user's input value
userAnswer = document.getElementById('userAnswer6').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message6');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message6');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer7() {
// Set the vaue of the underlined word
partOfSpeech = 'Cu';

// Get the user's input value
userAnswer = document.getElementById('userAnswer7').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message7');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message7');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer8() {
// Set the vaue of the underlined word
partOfSpeech = 'O';

// Get the user's input value
userAnswer = document.getElementById('userAnswer8').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message8');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message8');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer9() {
// Set the vaue of the underlined word
partOfSpeech = 'H';

// Get the user's input value
userAnswer = document.getElementById('userAnswer9').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message9');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message9');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer10() {
// Set the vaue of the underlined word
partOfSpeech = 'K';

// Get the user's input value
userAnswer = document.getElementById('userAnswer10').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message10');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message10');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer11() {
// Set the vaue of the underlined word
partOfSpeech = 'Na';

// Get the user's input value
userAnswer = document.getElementById('userAnswer11').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message11');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message11');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer12() {
// Set the vaue of the underlined word
partOfSpeech = 'Cl';

// Get the user's input value
userAnswer = document.getElementById('userAnswer12').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message12');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message12');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer13() {
// Set the vaue of the underlined word
partOfSpeech = 'Ne';

// Get the user's input value
userAnswer = document.getElementById('userAnswer13').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message13');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message13');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer14() {
// Set the vaue of the underlined word
partOfSpeech = 'Hg';

// Get the user's input value
userAnswer = document.getElementById('userAnswer14').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message14');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message14');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer15() {
// Set the vaue of the underlined word
partOfSpeech = 'Au';

// Get the user's input value
userAnswer = document.getElementById('userAnswer15').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message15');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message15');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer16() {
// Set the vaue of the underlined word
partOfSpeech = 'Br';

// Get the user's input value
userAnswer = document.getElementById('userAnswer16').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message16');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message16');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer17() {
// Set the vaue of the underlined word
partOfSpeech = 'Sn';

// Get the user's input value
userAnswer = document.getElementById('userAnswer17').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message17');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message17');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer18() {
// Set the vaue of the underlined word
partOfSpeech = 'Li';

// Get the user's input value
userAnswer = document.getElementById('userAnswer18').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message18');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message18');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer19() {
// Set the vaue of the underlined word
partOfSpeech = 'S';

// Get the user's input value
userAnswer = document.getElementById('userAnswer19').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message19');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message19');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer20() {
// Set the vaue of the underlined word
partOfSpeech = 'Al';

// Get the user's input value
userAnswer = document.getElementById('userAnswer20').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message20');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message20');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer21() {
// Set the vaue of the underlined word
partOfSpeech = 'Ca';

// Get the user's input value
userAnswer = document.getElementById('userAnswer21').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message21');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message21');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer22() {
// Set the vaue of the underlined word
partOfSpeech = 'U';

// Get the user's input value
userAnswer = document.getElementById('userAnswer22').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message22');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message22');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer23() {
// Set the vaue of the underlined word
partOfSpeech = 'I';

// Get the user's input value
userAnswer = document.getElementById('userAnswer23').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message23');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message23');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer24() {
// Set the vaue of the underlined word
partOfSpeech = 'He';

// Get the user's input value
userAnswer = document.getElementById('userAnswer24').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message24');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message24');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer25() {
// Set the vaue of the underlined word
partOfSpeech = 'Co';

// Get the user's input value
userAnswer = document.getElementById('userAnswer25').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message25');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message25');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer26() {
// Set the vaue of the underlined word
partOfSpeech = 'Ag';

// Get the user's input value
userAnswer = document.getElementById('userAnswer26').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message26');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message26');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer27() {
// Set the vaue of the underlined word
partOfSpeech = 'Be';

// Get the user's input value
userAnswer = document.getElementById('userAnswer27').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message27');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message27');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer28() {
// Set the vaue of the underlined word
partOfSpeech = 'Mg';

// Get the user's input value
userAnswer = document.getElementById('userAnswer28').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message28');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message28');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer29() {
// Set the vaue of the underlined word
partOfSpeech = 'Mn';

// Get the user's input value
userAnswer = document.getElementById('userAnswer29').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message29');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message29');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer30() {
// Set the vaue of the underlined word
partOfSpeech = 'Pt';

// Get the user's input value
userAnswer = document.getElementById('userAnswer30').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message30');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message30');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer31() {
// Set the vaue of the underlined word
partOfSpeech = 'W';

// Get the user's input value
userAnswer = document.getElementById('userAnswer31').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message31');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message31');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer32() {
// Set the vaue of the underlined word
partOfSpeech = 'Ti';

// Get the user's input value
userAnswer = document.getElementById('userAnswer32').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message32');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message32');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer33() {
// Set the vaue of the underlined word
partOfSpeech = 'Pb';

// Get the user's input value
userAnswer = document.getElementById('userAnswer33').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message33');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message33');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer34() {
// Set the vaue of the underlined word
partOfSpeech = 'Ar';

// Get the user's input value
userAnswer = document.getElementById('userAnswer34').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message34');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message34');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer35() {
// Set the vaue of the underlined word
partOfSpeech = 'Ra';

// Get the user's input value
userAnswer = document.getElementById('userAnswer35').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message35');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message35');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}

function checkAnswer36() {
// Set the vaue of the underlined word
partOfSpeech = 'F';

// Get the user's input value
userAnswer = document.getElementById('userAnswer36').value;

// Compare the user's answer to the correct answer
if (partOfSpeech == userAnswer) {
    // Log a message to make sure your logic works
    console.log('They got it right!');

    // Flash a message to the user letting them know it's right
    message = document.getElementById('message36');
    message.innerHTML = 'That is the correct answer!';

} else {
    // Log a message to make sure your logic works
    console.log('You imbicile, that answer is wrong.');

    // Flash a message to the user letting them know it's wrong
    message = document.getElementById('message36');
    message.innerHTML = 'Nope, that is incorrect.';
  }
}
